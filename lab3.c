#include <stdio.h>

void main(void){
    float area;
    int base = 5;
    int high = 10;
    area = 0.5*base*high;
    //printf ("base = %d \n", base);
    //printf ("base = %d \n", base);
    //printf ("base = %.3f \n", area);
    printf ("base = %d \n area = %.3f", base, high,area);
}